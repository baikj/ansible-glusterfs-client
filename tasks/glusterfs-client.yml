- name: Adds glusterfs client and utilities
  apt: >
    pkg={{ item }}
    state=latest
  with_items:
    - glusterfs-client
    - nfs-common
    - fio

- name: Ensure the Gluster volume is mounted
  mount:
    name: "{{ gluster_mount_dir }}"
    src: "{{ gluster_servers[0] }}:/{{ gluster_brick_name }}"
    fstype: nfs
    opts: "defaults,noatime,nodiratime"
    state: mounted
